{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('user/_form.html.twig') }}
    {% if is_granted('ROLE_ADMIN') %}
        <td>{{ include('user/_delete_form.html.twig') }}</td>
    {% endif %}

    <a href="{{ path('user') }}" class="btn btn-info">Back to list</a>
{% endblock %}
